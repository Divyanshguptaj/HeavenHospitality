packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10 blocks lifecycle scripts by default. Only these packages are allowed
# to run install scripts — each one needs a native/engine build step.
onlyBuiltDependencies:
  - '@prisma/client'
  - '@prisma/engines'
  - prisma
  - esbuild
  - sharp
